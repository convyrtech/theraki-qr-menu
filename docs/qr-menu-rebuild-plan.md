# QR-меню The Raki — перестройка под язык основного сайта (v2)

> Атомарный план для автономного прогона через `/loop`.
> v2 — 2026-06-27, после состязательного аудита (исправлены противоречия словам владельца,
> ловушка с фото для /loop, мобильная вёрстка, FOUC, поломка колеса).
> Цель: меню QR должно выглядеть как меню основного сайта (`D:\РАКИСВЕЖЕЕ\12354`, страница `/menu`) —
> простые кремовые карточки 2026 года, те же шрифты/тени/анимации. **Маршрут-цель: `/menu`.**

---

## 0. Директивы владельца (verbatim — эталон; не переинтерпретировать)

- «изучи меню на основном сайте — визуал, анимации; переработай под такой же визуал с карточками как там; практически копипастнуть, не выдумывая лишнего; изучи design.md; практически всё что нужно уже есть».
- **Интро:** при КАЖДОМ открытии — зелёный экран «The Raki» (наш) → растворяется кляксами Роршаха (наша анимация) → под ним кремовое меню. **В самом меню зелёного/героя НЕТ.** Зелёный — «тот, что у нас». Интро = текущий hero минус 2 детали (нить-полоска + «меню»-стрелка), плюс растворение.
- **Карточки:** простые, как везде в 2026 и как на сайте. Открытие блюда «в пол-экрана» по тапу — не проблема, оставляем.
- **Колесо категорий — ОСТАВЛЯЕМ.**
- **Раки → 2 карточки: «Отварные» / «Жареные»** (решение владельца). Не доска размеров.
- **Без фото → напитки/соусы/гарниры = текст-список; еда без фото = карточка с временной заглушкой** (решение владельца). Вода и пиво — текст.
- **Контент — СТРОГО из `Пустой 5.docx`.** `menu.ts` сверён (0 выдумок) — перепроверить, ничего не добавлять.
- **Фото:** все кадры (5–10 на блюдо) в `D:\РАКИСВЕЖЕЕ\THE RAKI ALL`. **Фон НЕ вырезать** — белый студийный фон ложится под карточки (cover). Одинаковый ракурс, 1+ на блюдо. Нет фото → временная заглушка (Наталья дошлёт). Сопоставление — ЧЕРНОВОЕ, владелец сам сверит и подпишет позже.
- **QR = только просмотр**, заказ принимает официант (без корзины/оплаты/аккаунта).

**От сайта отступаем РОВНО в 2 местах:** (1) колесо вместо бокового рейла; (2) read-only без корзины/аккаунта. Всё прочее — буквальная калька, ничего не до-придумывать.

## 1. Жёсткие запреты (DON'T)

- ❌ Не рисовать у раков сетку/доску «Размер | шт/кг | цена» (текущий `RakiBlock`, лесенка S→XXL) — владелец от неё отказался.
- ❌ Не использовать вырезанные cutout-фото В КАРТОЧКАХ (фон не вырезать). Карточки = полные фото с белым фоном, `object-fit: cover`, без drop-shadow.
- ❌ Не брать зелёный `#06201f` из proto. Зелёный интро = наш текущий hero-градиент (`#11454c→#0b3237`, токен сайта `#0b3237`).
- ❌ Не ставить `sessionStorage`-гейт на интро (нужно при КАЖДОМ открытии).
- ❌ Не тянуть с сайта корзину/конфигуратор/AI-официанта/аккаунт/боковой рейл. QR = просмотр.
- ❌ Не добавлять `framer-motion` (анимации — CSS + IntersectionObserver).
- ❌ Не выдумывать контент/позиции/цены/описания — только из `Пустой 5.docx`/`menu.ts`. ◆ signature не возвращать.
- ❌ Не выдавать догадку по фото за факт. Не привязывать фото из архива как окончательное без сверки владельцем.
- ❌ Не ставить `[x]` по самооценке кода. Готовность фазы = проверяемые пиксели + `/visual-qa`.

## 2. Источники истины (читать перед работой)

| Что | Файл |
|---|---|
| План | `theraki-qr-menu/docs/qr-menu-rebuild-plan.md` |
| Меню сайта (живое) | `12354/src/components/pages/menu-editorial-page.tsx` (рендерит `EditorialSectionCard`) |
| DESIGN.md сайта | `12354/DESIGN.md` (Cream Editorial, токены, моушн) |
| CSS карточек сайта | `12354/src/app/globals.css` — `menu-editorial__section-card*` (~5630), секции (~5580–5630) |
| Интро-Роршах | `theraki-qr-menu/src/app/proto/components.tsx` → `Intro`/`INK_BLOTS` (~668–779) + `proto.css` `.pt-intro*` (~850) |
| Текущая цель | `theraki-qr-menu/src/app/menu/page.tsx` + `menu.css` |
| Контент | `theraki-qr-menu/src/data/menu.ts` + `Пустой 5.docx` (корень репо) |
| Фото | `theraki-qr-menu/public/images/*.webp` (полные + `cutout/`); `theraki-qr-menu/фото` (13 подписанных); архив `D:\РАКИСВЕЖЕЕ\THE RAKI ALL` (302) |

Опорные факты (проверено): framer-motion в QR НЕТ. Шрифты `Cormorant_Garamond`/`Manrope`/`JetBrains_Mono` подключены через `next/font` в `layout.tsx`; **Fraunces — через `@font-face` в globals.css** (не в layout). Крем сайта = `#f2e8d5`, ease = `cubic-bezier(0.22,1,0.36,1)`. У `menu.ts` нет полей `metadata.serving/weight` — мета-строку строить из `unit`.

---

## 3. Атомарные задачи (порядок сверху вниз; `/loop` берёт первую `- [ ]`)

### Фаза A — Интро (Роршах) на `/menu`
- [ ] A1. Превратить ТЕКУЩИЙ `mn__hero` в полноэкранный (100dvh) экран приветствия. Удалить ровно `mn__hero-thread` (нить+капля) и `mn__hero-hint` («меню»+стрелка). Сохранить `mn__hero-eyebrow` «Раковарня · Москва» + «The <em>Raki</em>» + `mn__hero-sub` «Карта раковарни». Фон = текущий зелёный градиент (`#11454c→#0b3237`), НЕ `#06201f`. Убрать SVG-волну `mn__hero-wave`.
- [ ] A2. Навесить растворение Роршахом: перенести маску `INK_BLOTS` из proto `Intro` (симметричные крупные органичные сливающиеся пятна — как 5 кадров-референс владельца, НЕ мелкие точки). Зелёный слой прорезается растущими кляксами → под ним виден кремовый каталог.
- [ ] A3. Перекрытие с первого кадра (без вспышки крема/FOUC): зелёный — `position: fixed`, сплошной фон (НЕ transparent), `z-index` выше каталога, рендерить первым узлом, `body { overflow: hidden }` пока интро не завершилось. Приёмка: скриншот первого кадра = сплошной зелёный.
- [ ] A4. Каждое открытие: НЕ ставить `sessionStorage` (удалить гейт при переносе). Тап/скролл/`pointerdown` — пропустить мгновенно. `prefers-reduced-motion` → сразу скрыть.
- [ ] A5. Починить показ кнопки колеса (`mn__catbtn`) под отсутствие полноэкранного hero: текущий триггер `past = scrollY > innerHeight*0.62` завязан на высоту hero → заменить на детерминированный (кнопка видна сразу после растворения интро / всегда). Приёмка: колесо доступно при любой высоте контента.
- **Приёмка A:** загрузка `/menu` ДВАЖДЫ → оба раза зелёный «The Raki» с подзаголовком → органичное растворение кляксами → кремовое меню без героя/волны/нити/«меню»-стрелки; первый кадр сплошной зелёный; колесо достижимо. `/visual-qa` 390×844.

### Фаза B — Кремовая оболочка (как на сайте)
- [ ] B1. Крем-токены `menu.css` к сайту: фон `#f2e8d5` (+alt `#ece3ce`), текст `#101c1e` / muted `rgba(16,28,30,.72)`, акцент приглушённый teal. Шрифты уже подключены.
- [ ] B2. Верхняя панель как на сайте, усечённая: слева «The <em>Raki</em>» + кнопка «Меню» (бургер → колесо). Справа НИЧЕГО (без корзины/аккаунта).
- [ ] B3. Контейнер каталога (мобильно): кремовый фон, ширина = вьюпорт минус малые поля, ритм секций (gap ~48–60px, тонкая верхняя граница секции, `scroll-margin-top`).
- **Приёмка B:** фон = `#f2e8d5` (проверяемо в DevTools), типографика Cormorant/Manrope/JetBrains. `/visual-qa` зелёный.

### Фаза C — Карточки (калька с `EditorialSectionCard`, МОБАЙЛ-ФЁРСТ)
- [ ] C1. Заменить кнопки `mn__dish` на карточки в стиле сайта, но мобильно: **1 колонка, фото СВЕРХУ** (aspect ~4:3, `object-fit: cover`, белый фон фото — НЕ cutout). Под фото: название (Cormorant ~28px) + мета (mono ≥11px капсом) + цена (Cormorant) + описание `note` (≤ ~3 строк, ≥14px). Мягкая тень + лёгкий подъём по тапу. Текст ≥16px, вся карточка — тач-цель ≥44px.
- [ ] C2. Отвязать cutout В КАРТОЧКАХ: где `DISH_PHOTO` → `/images/cutout/*`, заменить на полное фото `/images/*.webp` (белый фон). CSS `contain`→`cover`, убрать `drop-shadow`. (Колесо `CAT_FACE` — НЕ трогаем здесь, медальоны остаются.)
- [ ] C3. Мета-строку переписать заново из `unit` (НЕ из `group` — это мини-заголовок главы; `metadata.serving/weight` в данных нет).
- [ ] C4. Глифы остроты/томата (`ChiliIcon`/`TomatoIcon`) сохранить у `spicy` и рецептов с «помидор». ◆ не возвращать.
- [ ] C5. Тап по карточке → `DishDetail` (крупное фото + название + описание + цена + variants). Контент из `note`/`variants`, без выдумок.
- **Приёмка C:** на 390×844 карточки = 1 колонка, фото сверху без вырезанного фона; блюда видно; нет overflow (DevTools); `/visual-qa` зелёный.

### Фаза D — Блюда/разделы БЕЗ фото (решение владельца)
- [ ] D1. Напитки и мелкие позиции без фото → **компактный текст-список** (имя · `unit`/объём · цена; `variants`/`note` под тап; `group` = мини-заголовок). Применить к разделам: `soft` (Безалкогольные), `beer` (Пиво), `tea` (Чай и кофе), `sauces` (Соусы), `garnish` (Гарниры). Без фото-слота.
- [ ] D2. Еда без `photo` (блюда в `salads`/`mains`/`starters`/`hot`/`soups`/`desserts`) → карточка как в C, но с **временной кремовой заглушкой-эмблемой** вместо фото (читается как задумка, не «недогруз»). Помечены для замены — Наталья дошлёт фото.
- **Приёмка D:** напитки/соусы/гарниры = аккуратный список; еда без фото = карточка с заглушкой; единый кремовый язык; `/visual-qa`.

### Фаза E — Навигация (колесо)
- [ ] E1. Колесо оставить как навигацию по кнопке «Меню» (`CategoryWheel`, не ломать; cutout-медальоны `CAT_FACE` остаются). Scroll-spy подсветка активной категории. Сайтовый боковой рейл НЕ переносим (на мобиле его нет).
- **Приёмка E:** колесо открывается из кнопки, активная категория следует за скроллом, тап ведёт в раздел.

### Фаза F — Анимации прокрутки (как на сайте, на CSS)
- [ ] F1. Карточки и заголовки секций мягко проявляются при входе во вьюпорт (IntersectionObserver добавляет класс → CSS `opacity`/`translateY`, ease `cubic-bezier(0.22,1,0.36,1)`). Без framer-motion. `prefers-reduced-motion` → мгновенно. Без постоянного rAF в списке.
- **Приёмка F:** плавное появление как на сайте, без джанка; `/visual-qa`.

### Фаза G — Раки = 2 карточки (решение владельца)
- [ ] G1. Раздел «Раки» = **2 карточки** в общем стиле: «Раки отварные» (фото варёных) и «Раки жареные» (фото жареных). На карточке: название + «от 3 900 ₽/кг» + короткая подпись. БЕЗ доски «Размер|шт/кг|цена».
- [ ] G2. Тап по раки-карточке → деталь: общая таблица размеров S–XXL (`rakiChapter.sizes`: ₽/кг + шт/кг) + рецепты этого способа из `rakiChapter.preparations` (отварные: Классический/Острый перчик🌶/Донской/Дон с помидором🍅; жареные: на крафтовом пиве с миксом трав🌶/в сливочно-чесночном) с глифами.
- **Приёмка G:** в меню 2 раки-карточки; деталь показывает размеры+рецепты из `rakiChapter` дословно; тёмной доски нигде нет.

### Фаза H — Фото (черновое сопоставление, владелец сверяет)
- [ ] H1. **Ручной/осторожный препроцесс (НЕ слепой автономный шаг):** контакт-листы `THE RAKI ALL` (302) с номерами кадров (ffmpeg `drawtext` + tile) в `scratchpad` + `index_map.tsv` (индекс↔DSC).
- [ ] H2. Стартовать с готового: 13 подписанных в `/фото` + 9 привязок в `DISH_PHOTO` + готовые `public/images/*.webp`. Архив 302 — только для блюд без фото.
- [ ] H3. Проход по каждому листу с записью в TSV: «блюдо → кадр-гипотеза → уверенность». Только high-confidence → ресайз ~860–1000px, webp q72–80, **≤~120 КБ**, `loading=lazy` → `public/images`; проставить `photo` в `menu.ts`. Фон НЕ вырезать.
- [ ] H4. Финал H = **сверочный лист** (блюдо → предложенный DSC → миниатюра) для владельца. Без подтверждения привязка НЕ финальная. Блюда без уверенного матча → кремовая заглушка (D2).
- **Приёмка H:** сматченные фото в карточках (cover, белый фон); лист на сверку существует; догадки не выданы за факт. **Это стоп-точка для владельца.**

### Фаза I — Проверка и полировка
- [ ] I1. `tsc`/`eslint` 0; `npm run build` ок (`output: "export"` не сломан).
- [ ] I2. `/visual-qa` в КАЖДОЙ визуальной фазе (B, C, D, F, G) + финально. Проверяемые числа, не самооценка.
- [ ] I3. `menu.ts` ↔ `Пустой 5.docx` пословно (docwords-покрытие), 0 выдумок.
- [ ] I4. Регресс: интро при каждом открытии + первый кадр зелёный; нет героя/волны/нити/«меню»-стрелки; колесо живо и достижимо; крем един; в карточках нет вырезанного фона; деталь работает; раки = 2 карточки.
- [ ] I5. Владелец на реальном телефоне `http://192.168.1.102:3010/menu` (dev на `0.0.0.0:3010`) — это истина, не оценка субагента.

---

## 4. Протокол `/loop`

1. Прочитать этот файл. Взять первую невыполненную `- [ ]` по порядку.
2. Прочитать связанные источники (§2) перед правкой. Менять минимум, без посторонних рефакторов.
3. Реализовать одну атомарную задачу.
4. Проверить: `tsc`/`eslint` чисто И пикселями 390×844; для визуальных фаз (B,C,D,F,G) — `/visual-qa`. НЕ ставить `[x]` по самооценке кода.
5. Отметить `- [x]`, закоммитить атомарно (`feat(menu): …`) на ветке `feat/site-aligned-menu`.
6. Повтор.

**СТОП (вернуть управление владельцу):** все `- [x]`; ИЛИ дошли до H4 (сверка фото); ИЛИ нужен ввод (спорный визуал, недостающие заглушки); ИЛИ `/visual-qa` красный и не правится за итерацию (описать, спросить).

**Без владельца НЕ делать:** окончательную привязку фото, мерж ветки, смену `MENU_URL`/деплой, любой контент не из дока.

## 5. Состояние веток
- База-откат до «awwwards»: коммит `b0f87c9`.
- Рабочая ветка: **`feat/site-aligned-menu`** (от `feat/awwwards-polish` — сохранены колесо, `menu.ts`, доступ к интро в `proto/`).
